[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ms-starforce-optimizer"
version = "0.1.0"
description = "MapleStory TW star force simulator and cost calculator (V272 rules)"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["starforce"]
